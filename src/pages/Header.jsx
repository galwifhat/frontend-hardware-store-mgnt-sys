import { Link } from "react-router-dom";

const Header = () => {
  return (
    <div>
      <header className="mb-8">
        <h1 className="text-3xl font-bold">Hardware Store Management System</h1>
        <p>This is a hardware Store!</p>
      </header>
      <nav>Where links will go</nav>
    </div>
  );
};

export default Header;
